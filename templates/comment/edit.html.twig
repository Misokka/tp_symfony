{% extends 'base.html.twig' %}

{% block title %}Modifier le Commentaire
{% endblock %}

{% block body %}
	<div class="flex justify-center items-center min-h-screen bg-gray-100">
		<div class="bg-white shadow-md rounded p-8 w-full max-w-2xl">
			<h1 class="text-2xl font-bold mb-4 text-gray-700">Modifier le commentaire</h1>
			<div class="mb-4">
				{{ include('comment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
			</div>
			<div class="flex justify-between">
				<a href="{{ path('app_comment_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Retour à la liste</a>
				{{ include('comment/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

