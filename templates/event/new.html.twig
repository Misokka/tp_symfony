{% extends 'base.html.twig' %}

{% block title %}New Event
{% endblock %}

{% block body %}
	<div class="flex justify-center items-center min-h-screen bg-gray-100">
		<div class="bg-white shadow-md rounded p-8 w-full max-w-md">
			<h1 class="text-2xl font-bold mb-6 text-gray-700">Créer un nouvel evennement</h1>

			{{ include('event/_form.html.twig', { 'button_label': 'Créer' }) }}

			<a href="{{ path('app_event_index') }}" class="block mt-4 text-blue-500 hover:underline">
				Retour a la liste
			</a>
		</div>
	</div>
{% endblock %}

