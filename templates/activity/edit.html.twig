{% extends 'base.html.twig' %}

{% block title %}Modifier l'Activité
{% endblock %}

{% block body %}
	<div class="flex justify-center items-center min-h-screen bg-gray-100">
		<div class="bg-white shadow-md rounded p-8 w-full max-w-2xl">
			<h1 class="text-2xl font-bold mb-4 text-gray-700">Modifier l'activité</h1>
			<div class="mb-4">
				{{ include('activity/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
			</div>
			<div class="flex justify-between">
				<a href="{{ path('app_activity_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 mr-2">Retour à la liste</a>
				{{ include('activity/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}

