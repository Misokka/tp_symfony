{% extends 'base.html.twig' %}

{% block title %}New Location
{% endblock %}

{% block body %}
	<div class="flex justify-center items-center min-h-screen bg-gray-100">
		<div class="bg-white shadow-md rounded p-8 w-full max-w-md">
			<h1 class="text-2xl font-bold mb-6 text-gray-700">Nouveau lieu</h1>

			{{ include('location/_form.html.twig', { 'button_label': 'Créer' }) }}

			<a href="{{ path('app_location_index') }}" class="block mt-4 text-blue-500 hover:underline">
				Retour à la liste
			</a>
		</div>
	</div>
{% endblock %}

